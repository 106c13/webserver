<parameter> <value>;

listen <port>; // listens <port> for http requests, default 8080

worker_connections <number>; // How many users can connect to the server simultaneously, default 10

root <path>; // path where html files are located

timeout <number>; // Time for waiting for connection in milliseconds

http_redirection <bool>; // I don't know how this work, but the idea is when user enters wrong website, it should redirect it to right one, like facebook.org redirect to facebook.com

directory_listing <bool>; // default NO, securiry reasons

default_file <file_name>; // When user sends request to / or /users/, which file should send server. By default it's index.html

upload_directory <path>; // Path where server will store uploaded files

// CGI ???
